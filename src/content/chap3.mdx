import Dialogue from '../components/mdx/Dialogue';
import BeforeAfter from '../components/mdx/BeforeAfter';

export const meta = {
  chapterId: 3,
  category: "環境構築・インフラ編",
  title: "CMakeを使ったターゲットビルドとテスト（CTest）の統合",
  description: "生の手書きMakefileと格闘するのはやめましょう。CMakeを使って、プロダクトコードとテストコードを綺麗に分離してビルドし、CTestで一括実行するモダンな設計を学びます。",
  responsibility: "CMakeビルドスクリプト構築・CTest統合",
  evolution: "手書きMakefileの依存関係地獄から解放され、コマンド一発で全テストバイナリを一括実行可能に！",
  tags: ["#CMake", "#CTest", "#ビルド自動化"]
};

<Dialogue character="penguin" title="ペンギンくんの憂鬱">
  「先生、環境は揃ったんで、とりあえずMakefile書きますね。えーっと、タブとスペース間違えるとエラーになるから慎重に……あぁっ、依存関係書くのめんどくせぇ！」
</Dialogue>

<Dialogue character="shirokuma" title="シロクマ先生の指導">
  「ストップだよ、ペンギンくん！現代のC++開発で生の手書きMakefileと格闘するのは時間の無駄だ。今は **CMake** を使って、プロダクトコードとテストコードを綺麗に分離してビルドし、**CTest** で全テストを一括自動実行するのがデファクトスタンダードだよ」
</Dialogue>

---

## 1. そもそも「CMake」とは何か？（コンパイラとの決定的な違い）

C++の勉強を始めた人が最も混乱するのが、**「コンパイラ」と「ビルドツール」と「CMake」の違い**です。関係性を整理しましょう。

| ツール区分 | 代表例 | 役割と立ち位置 |
| :--- | :--- | :--- |
| **コンパイラ** | `g++`, `clang++`, `MSVC` | **職人**。1つの `.cpp` ファイルを読み込み、機械語のオブジェクトファイル（`.o` や `.obj`）に翻訳する。 |
| **ビルド自動化ツール** | `Make`, `Ninja` | **現場監督**。どのファイルをどの順番でコンパイルし、どう結合（リンク）するかを指示書（`Makefile`）に従って実行する。 |
| **メタビルドツール** | **`CMake`** | **総合設計士**。現場監督のための「指示書（MakefileやVisual Studioプロジェクトファイル）」を、OSや環境に合わせて**自動生成**する。 |

```
【人間の入力】           【CMakeの仕事】               【ビルド実行】
CMakeLists.txt ──(cmake)──> Makefile / Ninjaファイル ──(make)──> 実行バイナリ (.exe / .so)
```

### なぜ手書きの Makefile ではダメなのか？
1. **OS互換性の壁**: Windows、Linux、Macでパスの区切り文字（`\` と `/`）やコンパイラフラグが異なり、手書きMakefileは他OSで確実に壊れます。
2. **依存関係の地獄**: ヘッダーファイル（`.h`）が増えるたびに手動で依存関係を更新しなければならず、ヒューマンエラーの温床になります。
3. **CMakeなら1つの設計図で解決**: `CMakeLists.txt` を1つ書くだけで、Linuxなら `Makefile`、Windowsなら `Visual Studio (.sln)` を自動生成してくれます。

---

## 2. そもそも「CTest」とは何か？（なぜテストランナーが必要か）

テストコードが増えてくると、`test_math`、`test_filter`、`test_network` のように**テスト用の実行ファイルが何十個も作られる**ようになります。

これを毎回手動で1つずつ `./test_math`, `./test_filter` ... と叩いて回るのは不可能です。

> **CTestとは？**<br/>
> CMakeに最初から標準搭載されている**「テスト一括自動実行＆結果集約マネージャー」**です。

* **一括実行**: 登録された全テストを1コマンドで並列実行する。
* **合否判定サマリー**: どこで何個のテストが落ちたかを「100% tests passed, 0 tests failed」のように即座に集計レポートする。
* **CI/CD連携**: GitHub Actionsなどの自動パイプラインでは、`ctest` の終了コード（0なら成功、1なら失敗）を見てデプロイの可否を自動判定する。

---

## 3. 「ライブラリ」と「テスト」を分離する設計思想

テストしやすいC++設計の鉄則は、**「`main()` 関数を持つ実行ファイルにすべての処理を書かない」**ことです。

* **コアロジック**: ライブラリ（静的ライブラリ `.a` や共有ライブラリ `.so`）としてビルドする。
* **本番アプリ**: コアライブラリをリンクして `main()` から呼び出す。
* **テストアプリ**: 同じコアライブラリをリンクして、テストフレームワーク（GoogleTest）から関数を直接テストする。

---

## 4. 実務で使える CMakeLists.txt のテンプレート

プロジェクトのルートに配置する `CMakeLists.txt` の書き方を見てみましょう。

```cmake
cmake_minimum_required(VERSION 3.14)
project(SonarAutomationLab CXX)

# C++17規格を要求
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# ---------------------------------------------------
# 1. プロダクトコード（コアロジック）をライブラリ化
# ---------------------------------------------------
add_library(sonar_core
    src/sonar_filter.cpp
    src/math_utils.cpp
)
target_include_directories(sonar_core PUBLIC include)

# ---------------------------------------------------
# 2. CTestの有効化とテストターゲットの作成
# ---------------------------------------------------
enable_testing() # ★これを書くだけでCTestが有効になる！

# テスト用実行ファイルの定義
add_executable(sonar_test
    tests/test_sonar_filter.cpp
)

# コアロジックとGoogleTestをリンク
target_link_libraries(sonar_test
    PRIVATE sonar_core
    PRIVATE gtest gtest_main pthread
)

# ★CTestにテストとして登録
add_test(NAME AllUnitTests COMMAND sonar_test)
```

---

## 5. 実行してみよう：CMakeとCTestの基本3ステップ

ターミナルで実際にビルドとテストを回すコマンドは、現代のCMakeでは以下の**3行だけ**です。

```bash
# 1. 設定フェーズ：buildディレクトリを作り、Makefileを自動生成
cmake -B build

# 2. ビルドフェーズ：ソースコードをコンパイル＆リンク
cmake --build build

# 3. テストフェーズ：CTestで全テストを一括自動実行！
ctest --test-dir build --output-on-failure
```

> [!TIP]
> `ctest` に `--output-on-failure` を付けると、**失敗したテストの詳細ログだけを画面に出力**してくれます。何百個もテストがある実務では必須のオプションです。
