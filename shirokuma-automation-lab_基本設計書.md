# シロクマ C++開発自動化ラボ 基本設計書

## 1. プロジェクト概要
- **プロジェクト名**: shirokuma-automation-lab (シロクマ C++開発自動化ラボ)
- **ドメイン**: `https://shirokuma-auto-cpp.jp`
- **目的**: C++開発における泥臭い手作業（ビルド環境構築、テスト、メモリ検査、デプロイ）を、PythonやDocker、CI/CDツールを活用して完全自動化する手法を提供する実践的学習プラットフォーム。
- **ターゲット**: C++の基礎文法は習得済みで、実際の現場での開発効率化やインフラ・テスト構築を学びたいエンジニア。

## 2. システムアーキテクチャ
### 2.1. 技術スタック
- **フロントエンド・フレームワーク**: React 18, Vite
- **開発言語**: TypeScript
- **スタイリング**: Tailwind CSS
- **ルーティング**: React Router v6
- **アイコン**: Lucide React

### 2.2. インフラ・デプロイ環境
- **ホスティング**: Cloudflare Pages
- **ドメイン管理**: ムームードメイン
- **CI/CD**: GitHub と Cloudflare Pages の連携による全自動ビルド＆デプロイ
- **SPA対応**: `public/_redirects` (`/* /index.html 200`) を設定し、クライントサイドルーティングの404エラーを防止。

## 3. 画面構成（UI/UX）
- **デザインテーマ**: ダークモードベース（Navy背景 × Cyan/Emeraldのアクセントカラー）。実務を意識したハッカーライクでモダンな開発者向けUI。
- **主要画面**:
  1. **トップページ (`/`)**:
     - ヒーローセクション（自動化コンセプトの提示）
     - 「自動化」の具体例を示す6つの機能カード（Docker, Python/pybind11, GitHub Actions, AddressSanitizer 等）
     - 姉妹サイト（STEP1〜4）への学習ロードマップナビゲーション
  2. **チャプターページ (`/chapter/:id`)**:
     - 第1章〜第12章までの学習コンテンツを表示するメインビュー。
  3. **コード実行ラボ (`/lab`)**:
     - 対話型ターミナルシミュレータ。実際のコマンドを入力し、擬似的な実行ログを体験できるキラーコンテンツ。

## 4. コンポーネント設計
| コンポーネント名 | 役割 |
|---|---|
| `App.tsx` | アプリケーションのルート。ルーティング（Routes）および、スマホ用ドロワーメニューのState管理を行う。 |
| `Header.tsx` | 上部ナビゲーション。パンくずリスト、コード実行ラボへのリンク、スマホ用ハンバーガーボタンを配置。 |
| `Sidebar.tsx` | 左側ナビゲーション。全チャプターへのリンク、姉妹サイトへの外部リンクを配置。スマホ表示時はオーバーレイのドロワーとして機能。 |
| `TopPage.tsx` | ランディングページ。サイトコンセプトとロードマップを提示。レスポンシブ対応済。 |
| `ChapterContent.tsx` | 各章のコンテンツ（マークダウン・コンポーネント）を動的に読み込み、レンダリングする。 |
| `CodeLab.tsx` | VS Code風のファイルツリーと、入出力をエミュレートするターミナル画面（コマンドエンジン内包）。 |

## 5. 主要機能要件
1. **モバイル・レスポンシブ対応**:
   - PC時はサイドバー固定（2カラムレイアウト）。
   - スマホ（モバイル）時はサイドバーを隠し、ヘッダーのトグルボタンでスライドインするドロワーUIを採用（`App.tsx` にて `isMobileMenuOpen` を一元管理）。
2. **シミュレーション機能（擬似コマンドエンジン）**:
   - `CodeLab.tsx` 内部で `history` ステートを配列で管理。
   - ユーザーの入力を検知し、数ミリ秒〜数秒のラグを意図的に発生させ、`g++`, `pytest`, `docker build` などのコンパイルやテストの実行ログを色付きでリアリティ高く出力する。
3. **姉妹サイト群とのエコシステム連携**:
   - 「シロクマC++ラボ」「シロクマQt×C++ラボ」「水中音響・ソナー技術入門」との相互リンク（STEP1〜4の導線）を明確に配置。
4. **SEO最適化**:
   - `index.html` にて、`title`、`meta description`、`canonical`、および `Open Graph (OGP)` タグを完全設定。

## 6. ディレクトリ構造
```text
shirokuma-react-lab/
├── public/
│   ├── _redirects         # Cloudflare Pages用SPAルーティング設定
│   └── images/            # キャラクター画像等のアセット
├── src/
│   ├── components/
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   ├── TopPage.tsx
│   │   ├── ChapterContent.tsx
│   │   └── CodeLab.tsx    # コード実行ラボ
│   ├── App.tsx            # ルーティング定義
│   ├── main.tsx
│   └── index.css          # Tailwindエントリポイント
├── index.html             # SEO・OGPメタタグ定義
├── package.json
├── tailwind.config.js
├── tsconfig.json
└── vite.config.ts
```
